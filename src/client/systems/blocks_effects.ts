import { World } from "@rbxts/matter";
import { Agency } from "client/agency";
import { Controls } from "client/controls";

export function blocks_effects(world: World, controls: Controls, agency: Agency): void {
	for (const [] of world.query()) {
	}
}
